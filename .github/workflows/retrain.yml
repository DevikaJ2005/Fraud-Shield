name: Daily Retraining

on:
  workflow_dispatch:

jobs:
  retrain:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Future Kaggle training hook
        run: echo "Manual placeholder only; automatic retraining is intentionally disabled."
